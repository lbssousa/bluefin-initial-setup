# Camada estática para IDEs e todos os processos da sessão systemd.
# Lido pelo systemd --user na inicialização; herdado por GNOME Shell, VSCode, Zed, etc.
SSH_AUTH_SOCK=$HOME/.var/app/com.bitwarden.desktop/data/.bitwarden-ssh-agent.sock
